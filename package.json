{
  "name": "cv",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "homepage": "https://pfalkowski.github.io/cv/",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run check:locales && astro build && npm run check:build",
    "preview": "astro preview",
    "astro": "astro",
    "check:locales": "node scripts/check-locales.mjs",
    "check:build": "node scripts/check-build.mjs"
  },
  "dependencies": {
    "astro": "^7.2.2"
  }
}
